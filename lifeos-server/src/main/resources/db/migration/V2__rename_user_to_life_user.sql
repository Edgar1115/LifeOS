-- =====================================================
-- V2: 表名 user → life_user
-- 说明：user 是 SQL 保留字/常见词，统一业务表用 life_ 前缀，避免歧义与关键字冲突。
-- 仅在本地已执行过 V1 的环境生效（V1 建的是 user 表）。
-- =====================================================

ALTER TABLE `user` RENAME TO `life_user`;